name: wb # tmuxinator rubygem
root: ~/Workspace/webuild
windows:
  - express:
      layout: main-vertical
      panes:
        - git pull && ls && npm update -g npm && npm outdated --depth 0
        - grunt
        - ./run.sh
